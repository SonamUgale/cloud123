🔥 ASSIGNMENT 6 — DEPLOY WEB APPLICATION USING PAAS (RENDER)
COMPLETE FINAL STEP-BY-STEP PRACTICAL SHEET
This is the final clean execution flow for deploying a Flask web application on a PaaS cloud platform using Render.

🎯 AIM
To deploy a Flask web application on a PaaS (Platform as a Service) cloud platform.

🧠 REQUIREMENTS
✅ Python Installed ✅ Git Installed ✅ GitHub Account ✅ Render Account ✅ Internet Connection

🌐 COMPLETE FLOW
Create Flask App
↓
Create requirements.txt
↓
Create Procfile
↓
Test Application Locally
↓
Push Code to GitHub
↓
Connect GitHub to Render
↓
Deploy Application
↓
Access Live Website

✅ STEP 1 — CREATE PROJECT FOLDER
Open terminal.
Run:
mkdir flaskapp

✅ STEP 2 — ENTER PROJECT FOLDER
cd flaskapp

✅ STEP 3 — INSTALL FLASK
pip3 install flask

🎯 WHAT THIS DOES
Installs Flask web framework.

✅ STEP 4 — CREATE main.py
touch main.py

✅ STEP 5 — OPEN main.py
nano main.py

✅ STEP 6 — ADD FLASK CODE
Paste:
from flask import Flask

app = Flask(__name__)

@app.route('/')
def home():
    return "Hello from Cloud PaaS Deployment"

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)

🎯 IMPORTANT
host='0.0.0.0'
allows internet access to app.

✅ STEP 7 — SAVE FILE
Press:
* CTRL + O
* Enter
* CTRL + X

✅ STEP 8 — CREATE requirements.txt
⚠️ IMPORTANT: Do NOT use:
pip freeze > requirements.txt
because it may include unwanted macOS packages.

✅ CREATE FILE MANUALLY
Run:
nano requirements.txt

ADD ONLY THIS
Flask

SAVE FILE
Again:
* CTRL + O
* Enter
* CTRL + X

🎯 WHAT IS requirements.txt?
Contains: ✅ Python dependencies needed for deployment.

✅ STEP 9 — CREATE Procfile
Run:
touch Procfile

✅ STEP 10 — OPEN Procfile
nano Procfile

✅ ADD THIS CONTENT
web: python3 main.py

🎯 WHAT IS Procfile?
Tells Render: ✅ how to start application.

✅ STEP 11 — SAVE FILE
Press:
* CTRL + O
* Enter
* CTRL + X

✅ STEP 12 — TEST APPLICATION LOCALLY
Run:
python3 main.py

🎉 EXPECTED OUTPUT
Running on http://127.0.0.1:5000

✅ STEP 13 — OPEN IN BROWSER
Visit:
http://127.0.0.1:5000

🎉 EXPECTED OUTPUT
Hello from Cloud PaaS Deployment

✅ STEP 14 — STOP SERVER
Press:
CTRL + C

✅ STEP 15 — CREATE GITHUB REPOSITORY
Open:
GitHub
Create new repository.
Example:
cloudPr

✅ STEP 16 — INITIALIZE GIT
Run:
git init

✅ STEP 17 — ADD FILES
git add .

✅ STEP 18 — COMMIT FILES
git commit -m "Initial commit"

✅ STEP 19 — CONNECT GITHUB REPOSITORY
Run:
git remote add origin YOUR_GITHUB_REPO_URL
Example:
git remote add origin https://github.com/SonamUgale/cloudPr.git

✅ STEP 20 — PUSH CODE TO GITHUB
Run:
git push -u origin master
OR:
git push -u origin main
(depending on branch)

⚠️ IMPORTANT
GitHub password authentication is removed.
Use: ✅ Personal Access Token (PAT)
instead of GitHub password.

🎉 CODE PUSHED SUCCESSFULLY

✅ STEP 21 — OPEN RENDER
Open:
Render
Login using GitHub account.

✅ STEP 22 — CREATE WEB SERVICE
Click:
New +
Choose:
Web Service

✅ STEP 23 — CONNECT GITHUB REPOSITORY
Select repository:
cloudPr
Click:
Connect

✅ STEP 24 — CONFIGURE DEPLOYMENT
Fill:
Field	Value
Name	cloudpractical
Runtime	Python 3
Build Command	pip install -r requirements.txt
Start Command	python3 main.py
SELECT PLAN
Choose:
Free Plan

✅ STEP 25 — DEPLOY APPLICATION
Click:
Deploy Web Service

🎯 WHAT HAPPENS?
Render: ✅ clones GitHub repository ✅ installs Flask ✅ starts Flask app ✅ hosts application online

⏳ WAIT
Deployment may take:
* 2–5 minutes

✅ STEP 26 — OPEN LIVE WEBSITE
Render generates URL like:
https://cloudpractical.onrender.com
Open it.

🎉 FINAL OUTPUT
Hello from Cloud PaaS Deployment

🎉 PRACTICAL COMPLETED
You successfully: ✅ created Flask app ✅ configured deployment files ✅ pushed code to GitHub ✅ connected Render ✅ deployed live application

🔥 IMPORTANT FILES
File	Purpose
main.py	Flask application
requirements.txt	dependencies
Procfile	startup command
🔥 IMPORTANT CONCEPTS
Concept	Meaning
PaaS	Platform as a Service
Flask	Python web framework
Render	cloud deployment platform
Procfile	app startup configuration
⚠️ COMMON ERRORS + FIXES
Error	Cause	Fix
Build failed	wrong requirements.txt	keep only Flask
Invalid username/password	GitHub password removed	use PAT token
App crashed	wrong Procfile	check startup command
Blank page	Flask app not running	verify code
🎯 FINAL PRACTICAL FLOW
Create Flask App
↓
Create requirements.txt
↓
Create Procfile
↓
Push Code to GitHub
↓
Connect Render
↓
Deploy Application
↓
Access Live URL
This is the complete final execution flow for PaaS Web Application Deployment practical.



# 🔥 HOW TO EXPLAIN THIS PRACTICAL TO EXAMINER

## 🎯 INTRODUCTION (Start Like This)

“Good morning sir/ma’am.
In this practical, I deployed a Flask web application on a PaaS cloud platform using Render.

The objective was:

* to create a Flask application,
* configure deployment files,
* push code to GitHub,
* connect the repository with Render,
* and deploy the application online.”

---

# 🔥 STEP-BY-STEP EXPLANATION TO EXAMINER

---

# ✅ STEP 1 — CREATED PROJECT FOLDER

“I first created a project folder named:
flaskapp”

Command used:

```bash
mkdir flaskapp
```

🎯 Purpose:

* Stores all project files.

---

# ✅ STEP 2 — ENTERED PROJECT DIRECTORY

```bash
cd flaskapp
```

🎯 Purpose:

* Move inside project folder.

---

# ✅ STEP 3 — INSTALLED FLASK

```bash
pip3 install flask
```

🎯 Explanation:

* Flask is a lightweight Python web framework used to create web applications.

---

# ✅ STEP 4 — CREATED main.py FILE

```bash
touch main.py
```

🎯 Purpose:

* Main Flask application file.

---

# ✅ STEP 5 — OPENED main.py

```bash
nano main.py
```

---

# ✅ STEP 6 — ADDED FLASK APPLICATION CODE

Code used:

```python id="gf5mgh"
from flask import Flask

app = Flask(__name__)

@app.route('/')
def home():
    return "Hello from Cloud PaaS Deployment"

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)
```

---

# 🔥 CODE EXPLANATION FOR EXAMINER

| Code            | Meaning                         |
| --------------- | ------------------------------- |
| Flask(**name**) | Creates Flask app               |
| @app.route('/') | Defines homepage route          |
| home()          | Function executed on homepage   |
| return          | Sends response to browser       |
| host='0.0.0.0'  | Allows external internet access |
| port=5000       | Application runs on port 5000   |

---

# ✅ STEP 7 — SAVED FILE

“I saved the file using:
CTRL + O → Enter → CTRL + X”

---

# ✅ STEP 8 — CREATED requirements.txt

“I manually created requirements.txt file.”

Command:

```bash
nano requirements.txt
```

Content:

```text id="hh5nm8"
Flask
```

🎯 Explanation:

* requirements.txt contains Python dependencies required during deployment.

---

# ✅ WHY NOT USE pip freeze?

Explain if examiner asks:

“pip freeze may include unnecessary system packages, so for this practical I manually added only Flask.”

---

# ✅ STEP 9 — CREATED Procfile

Command:

```bash
touch Procfile
```

---

# ✅ STEP 10 — ADDED Procfile CONTENT

```text id="j0h2jt"
web: python3 main.py
```

🎯 Explanation:

* Procfile tells Render how to start the application.

---

# ✅ STEP 11 — TESTED APPLICATION LOCALLY

Command:

```bash
python3 main.py
```

🎉 Output:

```text id="ibqk1e"
Running on http://127.0.0.1:5000
```

---

# ✅ STEP 12 — VERIFIED IN BROWSER

“I opened:
[http://127.0.0.1:5000”](http://127.0.0.1:5000”)

🎉 Output:

```text id="7x7j8r"
Hello from Cloud PaaS Deployment
```

🎯 This confirmed:

* Flask application working locally.

---

# ✅ STEP 13 — STOPPED SERVER

```text id="73r1hl"
CTRL + C
```

---

# 🔥 NOW MOVING TO GITHUB

---

# ✅ STEP 14 — CREATED GITHUB REPOSITORY

“I created a GitHub repository named:
cloudPr”

🎯 Purpose:

* GitHub stores project source code online.

---

# ✅ STEP 15 — INITIALIZED GIT

```bash
git init
```

🎯 Purpose:

* Initializes local Git repository.

---

# ✅ STEP 16 — ADDED FILES

```bash
git add .
```

🎯 Purpose:

* Adds all project files for tracking.

---

# ✅ STEP 17 — COMMITTED FILES

```bash
git commit -m "Initial commit"
```

🎯 Purpose:

* Saves project snapshot in Git.

---

# ✅ STEP 18 — CONNECTED GITHUB REPOSITORY

```bash
git remote add origin YOUR_GITHUB_REPO_URL
```

Example:

```bash
git remote add origin https://github.com/SonamUgale/cloudPr.git
```

---

# ✅ STEP 19 — PUSHED CODE TO GITHUB

```bash
git push -u origin main
```

🎯 Important Point:

* GitHub password authentication removed.
* Personal Access Token (PAT) is used instead.

🎉 Result:

* Code uploaded successfully to GitHub.

---

# 🔥 NOW MOVING TO RENDER DEPLOYMENT

---

# ✅ STEP 20 — OPENED RENDER

“I logged into Render using GitHub account.”

🎯 What is Render?

* Render is a PaaS cloud platform used to deploy applications.

---

# ✅ STEP 21 — CREATED WEB SERVICE

“I clicked:
New + → Web Service”

---

# ✅ STEP 22 — CONNECTED GITHUB REPOSITORY

“I selected my GitHub repository:
cloudPr
and connected it with Render.”

---

# ✅ STEP 23 — CONFIGURED DEPLOYMENT

I entered:

| Field         | Value                           |
| ------------- | ------------------------------- |
| Name          | cloudpractical                  |
| Runtime       | Python 3                        |
| Build Command | pip install -r requirements.txt |
| Start Command | python3 main.py                 |

Selected:
✅ Free Plan

---

# ✅ STEP 24 — DEPLOYED APPLICATION

“I clicked:
Deploy Web Service.”

🎯 What Render does internally:

* clones GitHub repository
* installs dependencies
* starts Flask server
* hosts application online

---

# ✅ STEP 25 — OPENED LIVE WEBSITE

“Render generated a live URL.”

Example:

```text id="vv4y7x"
https://cloudpractical.onrender.com
```

🎉 Final Output:

```text id="52rf11"
Hello from Cloud PaaS Deployment
```

---

# 🔥 FINAL CONCLUSION FOR EXAMINER

“At the end of this practical:

* I successfully created a Flask web application,
* configured deployment files,
* pushed code to GitHub,
* connected Render platform,
* and deployed the application online using PaaS.”

---

# 🔥 POSSIBLE VIVA QUESTIONS WITH ANSWERS

---

# 1️⃣ What is PaaS?

PaaS stands for:
✅ Platform as a Service.

It provides:

* deployment platform,
* runtime environment,
* hosting infrastructure.

---

# 2️⃣ What is Flask?

Flask is:
✅ a lightweight Python web framework.

Used for:

* web application development.

---

# 3️⃣ What is Render?

[Render](https://render.com?utm_source=chatgpt.com) is:
✅ a cloud PaaS deployment platform.

Used for:

* hosting web applications online.

---

# 4️⃣ What is the purpose of requirements.txt?

requirements.txt contains:
✅ Python dependencies required for deployment.

---

# 5️⃣ What is Procfile?

Procfile defines:
✅ how the application should start on server.

---

# 6️⃣ Why did you use host='0.0.0.0'?

It allows:
✅ external internet access to Flask application.

---

# 7️⃣ What does @app.route('/') do?

It defines:
✅ homepage URL route.

---

# 8️⃣ What is GitHub used for?

GitHub is used for:
✅ source code hosting and version control.

---

# 9️⃣ Why is Git used?

Git is used for:
✅ version control and code management.

---

# 🔟 Why did you push code to GitHub?

Because Render deploys application:
✅ directly from GitHub repository.

---

# 1️⃣1️⃣ What happens internally during deployment?

GitHub repository
↓
Render clones code
↓
Installs dependencies
↓
Runs Flask app
↓
Hosts website online

---

# 1️⃣2️⃣ What is cloud deployment?

Cloud deployment means:
✅ hosting application on internet servers.

---

# 1️⃣3️⃣ What is the purpose of Build Command?

Build command installs:
✅ required dependencies.

Example:

```bash
pip install -r requirements.txt
```

---

# 1️⃣4️⃣ What is Start Command?

Start command launches:
✅ Flask application server.

Example:

```bash
python3 main.py
```

---

# 1️⃣5️⃣ Why use Free Plan?

Free plan provides:
✅ basic hosting for learning and practicals.

---

# 1️⃣6️⃣ What if deployment fails?

Possible causes:

* wrong requirements.txt
* incorrect Procfile
* syntax errors in code

---

# 1️⃣7️⃣ Difference between IaaS and PaaS?

| IaaS                 | PaaS                      |
| -------------------- | ------------------------- |
| User manages servers | Provider manages platform |
| More control         | Easier deployment         |
| Complex              | Simple                    |

---

# 1️⃣8️⃣ What services were used?

| Service | Purpose          |
| ------- | ---------------- |
| Flask   | Web application  |
| GitHub  | Code hosting     |
| Render  | Cloud deployment |

---

# 1️⃣9️⃣ What is web hosting?

Web hosting means:
✅ making website accessible over internet.

---

# 2️⃣0️⃣ What is cloud computing?

Cloud computing means:
✅ delivering computing services over internet.

---

# 🔥 SHORT 1-MINUTE SUMMARY FOR EXAMINER

“Sir/ma’am, in this practical I created a Flask web application, configured requirements.txt and Procfile, tested the app locally, pushed the code to GitHub, connected the repository with Render, and deployed the application online using a PaaS cloud platform.”

---

# 🔥 IMPORTANT KEYWORDS TO REMEMBER

| Keyword    | Meaning                    |
| ---------- | -------------------------- |
| PaaS       | Platform as a Service      |
| Flask      | Python web framework       |
| Render     | Cloud deployment platform  |
| Procfile   | Startup configuration      |
| GitHub     | Source code hosting        |
| Deployment | Hosting application online |


